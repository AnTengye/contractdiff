server:
  port: 8080
  
minio:
  endpoint: "xxx.temp.com"
  access_key: "xxx"
  secret_key: "xxx"
  bucket: "pdfdiff"
  use_ssl: true
  expire_days: 7
  
mineru:
  api_url: "https://mineru.net/api/v4"
  api_token: "xxx"
  model_version: "vlm"
  callback_url: ""
  seed: "contractdiff-seed"
  token_created_at: "2026-01-01T00:00:00+08:00"  # RFC3339 format, when token was created/refreshed
  token_valid_days: 14                            # Token validity period in days

notification:
  check_interval_hours: 12  # How often to check token expiration (default: every 12 hours)
  dingtalk:
    webhook_url: ""         # DingTalk robot webhook URL
    secret: ""              # Optional: for signed webhooks
  feishu:
    webhook_url: ""         # Feishu robot webhook URL
    secret: ""              # Optional: for signed webhooks
  
auth:
  jwt_secret: "mytestdiff"
  token_expire_hours: 24
  
users:
  - username: "admin"
    password: "admin123"
    tenant: "default"
  - username: "user1"
    password: "user123"
    tenant: "tenant1"
